while onTheTrain
  if iHearASickSoung
    myMasksStatus = true
  end
end

def iHearASickSoung(passengerNoises)
  # Given a collection of passengers' sounds ["coughing", "yawning", "sneezing", "singing Jamaican traditional folksong"]
  # If any of them are sick sounds: cough, yawn
  # return `true`; else, return `false`
end

function iHearASickSoung(passsengers Sounds) {
  for (let i = 0; i < passengersSoungs.length; i++) {
    if (passengersSoungs[i] === "coughing" || passengersSoungs[i] === "sneezing") {
      return true
    }
  }
  return false;
}

    }
  }
}